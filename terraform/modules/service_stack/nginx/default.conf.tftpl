upstream backend {
    %{ for backend in backends ~}
    server ${environment}-${project_name}-${backend}:80;
    %{ endfor ~}
}

server {
    listen 80;

    location / {
        proxy_pass http://backend;
    }
}

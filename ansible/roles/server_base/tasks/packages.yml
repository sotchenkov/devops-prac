- name: Install packages
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600
    pkg:
      - ca-certificates
      - curl
      - jq
      - unzip
      - chrony
      - ufw
      - nginx-full

apiVersion: v1
kind: Service
metadata:
  name: homelab-backend-service
  namespace: homelab
spec:
  selector:
    app.kubernetes.io/name: app-backend
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
